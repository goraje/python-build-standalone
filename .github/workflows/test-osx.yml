name: test osx

on:
  workflow_dispatch:

jobs:
  uttype-framework:
    runs-on: 'macos-13'
    steps:
      - name: List content
        run: |
          ls /System/Library/Frameworks/UniformTypeIdentifiers.framework
